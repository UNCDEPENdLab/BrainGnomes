# Get processing status for all subjects

Get processing status for all subjects

## Usage

``` r
get_project_status(scfg)
```

## Arguments

- scfg:

  a project configuration object as produced by `load_project` or
  `setup_project`

## Value

data.frame with one row per subject/session containing completion status
columns.
