# helper function get all unique sequence_ids for a given sqlite database file

helper function get all unique sequence_ids for a given sqlite database
file

## Usage

``` r
get_sequence_ids(db)
```

## Arguments

- db:

  a sqlite database file path
