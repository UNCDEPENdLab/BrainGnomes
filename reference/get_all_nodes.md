# helper for recursively collecting all nodes from a tree

helper for recursively collecting all nodes from a tree

## Usage

``` r
get_all_nodes(node)
```

## Arguments

- node:

  A data.tree Node object

## Value

A list of all descendant nodes including the node itself
