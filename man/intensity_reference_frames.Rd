% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess_functions.R
\name{intensity_reference_frames}
\alias{intensity_reference_frames}
\title{Identify volumes used to estimate the run reference intensity}
\usage{
intensity_reference_frames(
  in_file,
  confounds_file = NULL,
  censor_file = NULL,
  lg = NULL
)
}
\arguments{
\item{in_file}{Path to the input 4D BOLD NIfTI image.}

\item{confounds_file}{Optional path to an fMRIPrep confounds TSV file.}

\item{censor_file}{Optional path to a censor file containing one value per
volume, where a positive value means that the volume is included.}

\item{lg}{Optional logger for warnings about incompatible metadata.}
}
\value{
Logical vector with one value per BOLD volume. \code{TRUE} means that the
volume is used to estimate the intensity reference.
}
\description{
All volumes are included when no matching metadata are available. When an
fMRIPrep confounds table is present, volumes marked as non-steady-state are
excluded. A matching pipeline censor vector is also honored. These
exclusions affect estimation of the voxelwise temporal baselines and run
reference intensity; the eventual multiplier is still applied to every
volume in the run.
}
\keyword{internal}
