% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{derive_reference_core}
\alias{derive_reference_core}
\title{Derive a conservative functional reference core from 4D BOLD data}
\usage{
derive_reference_core(
  img,
  candidate_mask,
  constraint_mask = NULL,
  include_frames = NULL,
  baseline_method = "trimmed_mean",
  baseline_trim = 0.1,
  baseline_floor_fraction = 0.2,
  relative_noise_mad_cutoff = 5,
  spike_mad_cutoff = 6,
  max_spike_fraction = 0.05,
  min_valid_frames = 20L,
  affine_tolerance = 1e-05,
  outfile = ""
)
}
\arguments{
\item{img}{A 4D \code{RNifti::NiftiImage} object or path to a 4D BOLD NIfTI.
Intensities must retain a positive baseline; do not pass demeaned or
residualized data whose voxelwise means have been removed.}

\item{candidate_mask}{Required 3D functional candidate mask, normally from
\code{automask()} applied to the original positive-scale BOLD data with
\code{fill_holes = FALSE} and \code{dilate_steps = 0}.}

\item{constraint_mask}{Optional 3D mask used only as a subtractive
constraint. If \code{NULL} (the default), the candidate mask alone defines the
population subject to temporal quality refinement.}

\item{include_frames}{Optional logical vector with one value per volume.
\code{TRUE} marks steady-state, uncensored volumes eligible for estimation.
Missing values are not allowed. If \code{NULL}, all volumes are eligible.}

\item{baseline_method}{Either \code{"trimmed_mean"} (default) or \code{"median"}.}

\item{baseline_trim}{Fraction removed from each tail when
\code{baseline_method = "trimmed_mean"}. The default is 0.10.}

\item{baseline_floor_fraction}{Minimum baseline as a fraction of the spatial
median baseline in the mask intersection. The default 0.20 is provisional
and should be calibrated on representative data.}

\item{relative_noise_mad_cutoff}{Upper cutoff for log robust relative noise,
expressed as spatial median plus this many scaled MADs. The default is 5.}

\item{spike_mad_cutoff}{A consecutive-volume change is called extreme when
its absolute deviation from the median first difference exceeds this many
scaled MADs. Both volumes bordering an extreme change are marked. Default
is 6.}

\item{max_spike_fraction}{Maximum fraction of eligible volumes marked by an
extreme consecutive-volume change. Default is 0.05.}

\item{min_valid_frames}{Minimum number of finite eligible volumes required
for a voxel. At least two consecutive finite differences are also required.}

\item{affine_tolerance}{Absolute tolerance used when comparing the active
4-by-4 NIfTI transforms of the image and masks.}

\item{outfile}{Optional path for the final uint8 reference-core mask. If
\code{""}, no file is written.}
}
\value{
A list containing 3D RNifti images \code{core}, \code{agreement_mask},
\code{mask_membership}, \code{baseline}, \code{relative_noise_cv}, \code{spike_fraction},
\code{finite_fraction}, and \code{rejection_code}, plus scalar \code{thresholds}, voxel
\code{counts}, and \code{mask_metrics}. \code{mask_membership} is coded 0 = neither mask,
1 = mask A only, 2 = mask B only, and 3 = agreement. Temporal QA maps are
computed over the candidate-mask union when a constraint is supplied so
disagreement regions can be audited, but thresholds and the final core are
derived strictly from the effective candidate population. With no
constraint, \code{agreement_mask} is simply the candidate mask. Rejection codes
are additive bit flags: 1 = outside the effective candidate population,
2 = insufficient finite/consecutive observations, 4 =
nonfinite or nonpositive baseline, 8 = baseline below the relative floor,
16 = extreme relative noise, and 32 = excessive spike fraction.
}
\description{
Refines a functional candidate mask by removing voxels with an invalid or
very low robust baseline, extreme robust relative temporal noise, or an
excessive fraction of abrupt signal changes. An optional second mask can be
supplied as a subtractive constraint. This function never dilates a mask or
fills holes.
}
\details{
Robust relative noise is
\code{1.4826 * median(abs(diff(y))) / (sqrt(2) * baseline)}, calculated only
across consecutive eligible volumes. This median absolute first-difference
estimator remains sensitive to sustained alternating noise, for which MAD
around the median difference can collapse to zero. Its spatial upper
threshold is estimated on the log scale after the baseline criteria have
been applied. Spike fraction is the fraction of eligible volumes adjoining
an extreme first difference, with the spike threshold based on MAD around
the median difference. Threshold defaults are intentionally conservative
draft values and require empirical calibration before pipeline use.
}
