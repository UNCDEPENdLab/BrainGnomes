% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{select_list_safe}
\alias{select_list_safe}
\title{A Safe Version of \code{select.list()} for Interactive and TTY Use}
\arguments{
\item{choices}{A character vector of choices to present.}

\item{title}{Optional title string to display above the menu (like in \code{select.list()}).}

\item{multiple}{Logical; if \code{TRUE}, the user may select multiple items by entering space-separated numbers.}
}
\value{
A character string or character vector of selections. Returns an empty string (\code{""}) if the user cancels
by entering \code{0}. If \code{multiple = TRUE}, a character vector of selected items is returned.
}
\description{
Provides a cross-platform wrapper for R's \code{\link[=select.list]{select.list()}} that works both
in interactive R sessions and in non-interactive \code{Rscript} sessions at a terminal (TTY).
If running interactively, it calls \code{utils::select.list()}. If running in a non-interactive
terminal, it displays a textual menu and captures numeric input from the user.
}
\details{
This function is especially useful in command-line scripts that need user input
in a controlled and fallback-compatible way.
}
\examples{
\dontrun{
  select_list_safe(c("Apples", "Oranges", "Bananas"), title = "Select fruit:")
}
}
\seealso{
\code{\link[utils:select.list]{utils::select.list()}}
}
\keyword{internal}
