% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_postprocess.R
\name{get_postproc_stream_outputs}
\alias{get_postproc_stream_outputs}
\title{Determine expected output files for a postprocessing stream}
\usage{
get_postproc_stream_outputs(input_dir, input_regex, bids_desc)
}
\arguments{
\item{input_dir}{Directory containing the input NIfTI files to be
postprocessed.}

\item{input_regex}{Specification used to match the input files. This may be
a space-separated set of BIDS entities (e.g., "desc:preproc suffix:bold")
or a regular expression prefixed with "regex:".}

\item{bids_desc}{The \code{desc} value to use for the output filenames.}
}
\value{
A character vector of full paths to the expected postprocessed
NIfTI files. The vector is named with the corresponding input file.
}
\description{
Given a directory of candidate input files and a specification for
matching those inputs, this function returns the full paths to the
corresponding postprocessed NIfTI files. The output filenames are
derived by replacing the \code{desc} entity in each input file with the
stream's \code{bids_desc} value.
}
\examples{
\dontrun{
get_postproc_stream_outputs(
  input_dir = "/path/to/subject",
  input_regex = "desc:preproc suffix:bold",
  bids_desc = "clean"
)
}
}
